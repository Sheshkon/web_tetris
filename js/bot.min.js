import Position from"../js/position.min.js";const moves=["this.move(1, 0)","this.move(-1, 0)","this.move(0, 1)","this.rotate(true)","this.rotate(false)"];export default function botMove(){let t=getRandomInt(moves.length);return moves[t]}function getRandomInt(t){return Math.floor(Math.random()*t)}